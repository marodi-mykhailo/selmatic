import React from 'react';
import "./Dashboard.scss"

const Dashboard = () => {
    return (
        <div className={"dashboard"}>
            <h1>Dashboard</h1><span>podsumowanie & statystyki</span>
        </div>
    );
};

export default Dashboard;